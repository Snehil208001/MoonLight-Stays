# ===============================
# PRODUCTION
# ===============================

server.port=${PORT:5000}

# Azure App Service
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

app.seed.enabled=false
app.seed.on-empty-db=true

# Persist uploaded images on Azure's durable /home volume so they survive
# container restarts/redeploys (requires app setting WEBSITES_ENABLE_APP_SERVICE_STORAGE=true).
app.upload.dir=${APP_UPLOAD_DIR:/home/data/uploads}

frontend.url=${FRONTEND_URL:http://localhost:3000}

app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:}

jwt.secretKey=${JWT_SECRET_KEY}

stripe.secret.key=${STRIPE_SECRET_KEY:}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:}

# --- AI Trip Planner (Gemini) ---
gemini.api.key=${GEMINI_API_KEY:}
gemini.model=${GEMINI_MODEL:gemini-3.5-flash}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always